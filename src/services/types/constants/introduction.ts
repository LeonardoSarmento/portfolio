export type TIntroductionContent = {
  header: { initial: string; punchline: string };
  image: { src: string; alt: string };
  introimage: { src: string; alt: string };
  aboutmeimage: { src: string; alt: string };
  introduction: { h1: string; code: string };
  aboutme: string;
  description: string[];
};
